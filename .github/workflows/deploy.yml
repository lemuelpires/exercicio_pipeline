name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Vercel CLI
      run: npm i -g vercel

    - name: Deploy to Vercel
      run: vercel --token ${{ secrets.aWblZmkGBLDal9634Nv1EEYI }} --prod
      env:
        VERCEL_ORG_ID: ${{ secrets.QzlGBRoaxJEF7gLtA2IBMazo }}
        VERCEL_PROJECT_ID: ${{prj_HtewdEMhAhpi8uafzUfy6JIgGwXu}}
